= jQuery Nested Attributes plugin example

A simple Ruby on Rails project that uses jquery.nested_attributes.js to easily add/remove associated models

== Install

 $ sudo gem install bundler
 $ bundle install
 $ rake db:create db:migrate
 $ bundle exec rails s

== Additional notes
